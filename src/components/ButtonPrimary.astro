---
const { textColor, bgColor, bgHover, href } = Astro.props
---

<a href={href} class=`${textColor} ${bgColor} ${bgHover} focus:outline-none font-medium rounded-lg text-sm px-5 py-2.5 text-center inline-flex items-center transition-all duration-200`>
    <slot />
</a>